export type Product = {
  arrayData: {
    firstName: string;
    lastName: string;
    age: number;
    size: number;
    ss?:string
  }[];
  title: string;
};

export type ProfileProps = {
  name: string;
};
